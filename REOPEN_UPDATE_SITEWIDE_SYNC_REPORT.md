# Reopen Update Sitewide Sync Report

Reopen is handled as a shared backend lifecycle action and resets the status to `New`.

The action uses a branded confirmation modal before patching backend state.
