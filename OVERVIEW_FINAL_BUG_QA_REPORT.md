# Overview Final Bug QA Report

Fixed / verified:

- Hero video files still load from optimized assets.
- Ibiza time now renders without broken placeholder.
- Last Updated is populated.
- Start Here / internal links now switch app group before scrolling.
- Admin panels are hidden from Overview flow.
- Mobile width has no horizontal overflow in QA.
- Ask HORIZONS opens from Overview.

Browser link checks passed for:

- `#who-do-i-call`
- `#locations`
- `#menus`
- `#guests`
- `#report-inbox`

Remaining:

- Re-test production after deploy because live site currently still has pre-patch assets until push/deploy completes.
