# Report To Red Flags / Decisions Sync Report

Shared reports with `record_type: red_flag` now also render in Red Flags.

Shared reports with `record_type: decision` now also render in Decisions Needed.

Both use the same shared backend update ID, so lifecycle status changes sync through the original report record.
