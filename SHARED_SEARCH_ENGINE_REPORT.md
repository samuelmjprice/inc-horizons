# Shared Search Engine Report

Status: Fixed.

Homepage search and Ask HORIZONS now use the same search path instead of behaving like separate systems. Search avoids the excluded private attendee fields already listed in `content.json`.

Files changed: `script.js`, `content.json`.

