# Report Status Sitewide Sync Report

Report/update lifecycle actions now patch `/api/updates/:id`. Local UI is updated only after a successful backend response.

Status controls also require backend success before updating the local cache or status override.
